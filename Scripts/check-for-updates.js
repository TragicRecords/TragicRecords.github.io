$(document).ready(function () {
  checkForActiveEvent();
  checkForRecentNews();
});

function checkForActiveEvent() {
  // Replace the following line with your actual logic to determine if there's an active event.
  var isEventActive = false;

  if (isEventActive) {
    // Display an alert
    alert("There is an upcoming event! Check it out on the Events page.");
  }
}

function checkForRecentNews() {
  // Replace the following line with your actual logic to determine if there's a recent news post.
  var isRecentNews = true;

  if (isRecentNews) {
    // Display an alert
    alert("Recent news has been posted! Check it out on the News page.");
  }
}
